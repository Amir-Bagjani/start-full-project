export type ColorPreset = {
  contrastText: string;
  lighter?: string;
  light: string;
  main: string;
  dark: string;
  darker?: string;
  name: string;
};

export type PresetKey = 'default' | 'green' | 'cyan' | 'orange' | 'red';
